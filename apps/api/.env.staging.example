NODE_ENV=staging
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/fullpos_staging?sslmode=disable

# Safety note:
# Use an isolated staging database only. Never point fiscal concurrency or
# destructive validation scripts at production.
